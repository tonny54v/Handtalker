<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Handtalker"
        tools:targetApi="31">
        <activity
            android:name=".Activity_recupera_cuenta"
            android:exported="false" />
        <activity
            android:name=".Activity_configuration"
            android:exported="false" />
        <activity
            android:name=".Activity_handtalker_frases_predet"
            android:exported="false" />
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".VentanaBlancoActivity" />
        <activity android:name=".HandTalkerMainActivity" />
        <activity android:name=".Activity_handtalker_traduccion" />
        <activity android:name=".Activity_handtalker_camera" />
        <activity android:name=".Activity_handtalker_perfil" />
    </application>

</manifest>